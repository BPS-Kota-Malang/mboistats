import 'package:flutter/material.dart';

class EkonomiPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi dengan tampilan halaman Kependudukan
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman Ekonomi'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Ekonomi.'),
      ),
    );
  }
}
