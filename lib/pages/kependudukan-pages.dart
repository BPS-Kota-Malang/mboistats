import 'package:flutter/material.dart';

class KependudukanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi dengan tampilan halaman Kependudukan
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman Kependudukan'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Kependudukan.'),
      ),
    );
  }
}
