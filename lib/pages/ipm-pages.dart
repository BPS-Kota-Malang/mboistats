import 'package:flutter/material.dart';

class IpmPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi dengan tampilan halaman Kependudukan
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman IPM'),
      ),
      body: Center(
        child: Text('Ini adalah halaman IPM.'),
      ),
    );
  }
}
