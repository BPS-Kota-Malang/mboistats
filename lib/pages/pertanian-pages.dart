import 'package:flutter/material.dart';

class PertanianPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi dengan tampilan halaman Kependudukan
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman Pertanian'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Pertanian.'),
      ),
    );
  }
}
