import 'package:flutter/material.dart';

class KemiskinanPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi dengan tampilan halaman Kependudukan
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman Kemiskinan'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Kemiskinan.'),
      ),
    );
  }
}
