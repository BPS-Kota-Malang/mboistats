import 'package:flutter/material.dart';

class mboistatsIcon {
  final String icon;
  final String title;
  final Color? color;
  final String route;

  mboistatsIcon({
    required this.icon,
    required this.title,
    this.color,
    required this.route,
  });
}
